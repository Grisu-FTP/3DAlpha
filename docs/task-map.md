# Task map

Choose one row, search the listed files for the relevant symbol, then read a bounded range.
Paths under `src/core/` below are shortened. Paired `.hpp`/`.cpp` modules share the named stem.
Test filters match `TEST(...)` case names: find them with `rg -n '^TEST' tests/<file>`.
Do not run every filter in this table for a single change.

| Task | Start here | Tests / deeper reference |
|---|---|---|
| Entity save/reload | `entity/persistence`, `render/world_streamer` (`bindEntities`, `snapshotEntities`), `world/chunk_cache` (`applyPlayerState`), `src/impl/storage/alpha_chunkfiles/level_dat` | `tests/entity_persistence_test.cpp`; `current-work.md` for format limits |
| Arrow, painting, boat, minecart behaviour | `entity/<name>`, `item/use`, corresponding `render/<name>_mesh`; wiring in `src/platform/ctr/main.cpp` | `tests/<name>_test.cpp`; `entity-render-a1.1.2.md` |
| Animals (pig, sheep, cow, chicken) | `entity/mob` (the tick and the table), `entity/mob_spawn` (`az`), `entity/path_finder` (`cz`), `render/mob_mesh` (the models and `dn`'s transform), `entity/fire_entry` (what catches fire and what it costs, `kh.c(DDD)V`'s tail -- shared with every other pool), `entity/block_contact` (the block-collision loop at the head of that same tail; the cactus), `render/entity_fire_mesh` (the flames, `ak.a(Lkh;DDDF)V`); wiring in `src/platform/ctr/main.cpp` | `tests/mob_test.cpp`, `tests/fire_entry_test.cpp`, `tests/entity_fire_mesh_test.cpp`; **`mobs-a1.1.2.md`** is the derivation, `status.md` 23 the write-up. No breeding and no taming in this version -- animals despawn, with no exception |
| Monsters (zombie, skeleton, creeper, spider, slime) | The same four files -- they are five more rows of `MobDef` and one more `MobAi` branch each -- plus `entity/explosion` (`je`), `settings/world_settings` (the difficulty) and `entity/arrow` (mobs and the player are targets now) | `tests/monster_test.cpp`; **`mobs-a1.1.2.md`** *The five hostiles* is the derivation, `status.md` 25 the write-up and 27 the spawner's. **Where they spawn is a cave question**: `--spawns <world-dir>` measures it on a real world and the Info page shows the same counters. A fist leaves through `MobSurroundings::hurtPlayer` and lands in `PlayerVitals::attack` -- see the player-health row below |
| Item drops / pickup / merging | `entity/item_entity`, `tick/drop`, `item/inventory`, `entity/block_contact` (what a cactus does to a stack, `ly.b(Lcn;IIILkh;)V`); generated `data/a1.1.2/drops.json` | `tests/item_entity_test.cpp`, `drop_test.cpp`; case names contain behaviour, not always module name |
| Falling sand/gravel | `entity/falling_block`, `tick/behaviour` (`fallingTick`) | Search `tests/tick_test.cpp` for falling cases; persistence test covers unloaded restoration |
| TNT | `entity/primed_tnt` (`jd`), `entity/explosion` (`je`, strength 4), `tick/drop` (`tntDestroyedByPlayer`/`ByExplosion`), `tick/redstone` (`tntNeighbourChanged`), `tick/fire`, `render/primed_tnt_mesh`; wiring in `src/platform/ctr/main.cpp` | `tests/primed_tnt_test.cpp`. **Four ignition paths and all four are ported**: a break, a fire, a blast and a neighbour going live. Breaking TNT in a1.1.2 *lights* it and drops nothing -- `quantityDropped` is 0 and the override has no metadata guard, so crafting is the only way to obtain it |
| Mob spawner block (the dungeon cage) | `entity/mob_spawner` (`bd` -- the store and the tick), `render/spawner_mesh` (`r` -- the miniature, which rides the mob pass), `tick/behaviour` (`TickBehaviour::MobSpawner`, both halves of `jt`), `tick/tick_world` (the two tile-entity sinks), `render/world_streamer` (`setColumnSinks`); wiring in `src/platform/ctr/main.cpp` | `tests/mob_spawner_test.cpp`; `status.md` 38 is the write-up. **Not `az`/`k`** -- that is `entity/mob_spawn` and `status.md` 27. The mob is read out of the chunk's `TileEntities` and written back to it (`world/tile_entity`, `status.md` 39); `--spawns <copy> [ticks] at=x,y,z` stands the player on a real dungeon's cage and reports what it did |
| Tile entities (`TileEntities`) | `world/tile_entity` (`ic` and its four subclasses, plus `reconcileTileEntities`), `src/impl/storage/alpha_chunkfiles/{chunk_nbt,item_nbt}` (the codec), `render/world_streamer` (`setColumnSinks`'s `saving` callback and `markColumnModified`); the two session stores that ride it are `world/sign_store` and `entity/mob_spawner` | `tests/tile_entity_test.cpp`; `status.md` 39 is the write-up and `world-format.md` the layout. **Four tenants and they had to land together** -- writing the list without modelling the chest would empty every dungeon chest. `--rewrite <copy>` plus `tools/nbtdiff.py difftree` is the round-trip proof |
| Signs / text | `world/sign_store` (the store plus `readSigns`/`writeSigns`), `render/sign_mesh`, `item/use`, `src/platform/ctr/overlay.cpp` | `tests/sign_test.cpp`; the text persists -- it goes through the chunk's `TileEntities` (`world/tile_entity`, `status.md` 39) |
| Block/item use or placement | `item/use`, `block/registry`, `item/registry`, `tick/behaviour` (`canPlaceAt`) | `tests/use_test.cpp`, `placement_test.cpp`; generated `data/a1.1.2/{items,placement,selection}.json`. **Two placements ask less than the rest**: `World.canBlockBePlacedAt` skips the block's own rule for a cell holding water, lava, fire or a snow layer (which is how a triple chest is built), and `ItemSeeds.onItemUse` asks nothing at all, which is how a seed removes bedrock. Both are a1.1.2's -- `status.md` 50 |
| Held item / first-person hand | `render/held_item`, `Renderer::drawHeldItem` and `setHeldItem`, `editBlocks`/`hand` in `src/platform/ctr/main.cpp` | `tests/held_item_test.cpp`; `status.md` §15 for the near plane, depth map and stereo numbers |
| Top-screen messages (chat lines, entity-limit notice) | `gui/chat_log`, `render/chat_mesh`, `Renderer::drawChat`, `item::markRefusals`/`limitMessage`, `ChatSink` in `src/platform/ctr/main.cpp` | `tests/chat_log_test.cpp`; `status.md` §18; needs the pack font |
| Player skin / Skins screen | `texture/skin_list`, `texture/entity_skins` (`applyPlayerSkin`, `EntitySkin::Player`), `Menu::handleSkins`, `GameSettings::skin` | `tests/skin_list_test.cpp`, `entity_skins_test.cpp`; slim is gated on `hasSlimSkins` in `versions/<id>.json` |
| Inventory / creative palette / icons | `item/inventory`, `item/creative_palette`, `gui/item_icon`, `src/platform/ctr/overlay.cpp` | `tests/creative_test.cpp`, `item_icon_test.cpp`; item table from `tools/configure.py` |
| Where a world starts the player | `world/spawn_point` (`cn.g`, `cn.f`, the constructor's sand walk and `cn.a()`'s nudge, over a probe callback), `entity/player_body` (`liftOutOfGround` -- `kh.q()`), `Menu::createWorld` in `src/platform/ctr/menu.cpp` (the one call site), `respawnBody`/`liftIntoTheWorld`/`spawnLiftOwed` in `src/platform/ctr/main.cpp` | `tests/spawn_point_test.cpp`; `physics-a1.1.2.md` *Where a world starts you*, `status.md` 47. **y is never searched for** -- both loops move x and z, `spawnY` stays 64, and the lift is what puts the player on the surface. The lift is **owed** until the body's column is resident, and a player loaded out of `level.dat` is never lifted at all |
| Player health, damage, death and respawn | `entity/player_vitals` (`dm`/`ge`'s counters and the damage entry), `entity/damage_source`, `entity/player_body` (`landedFall`), `entity/item_entity` (`dropOnDeath`), `world/chunk_cache` (`applyPlayerState`); `PlayerHarm` and the death screen in `src/platform/ctr/main.cpp`, `Overlay::setDead` | `tests/player_vitals_test.cpp`; `physics-a1.1.2.md` *Survival*, `status.md` 46. **The window and "already dead" are checked in `dm` before `ge` sees the hit**, and difficulty scales only a monster's or an arrow's damage. `--survive <copy>` is the end-to-end check |
| Break progress, harvest drops, tool wear | `item/block_breaking` (`nj`'s loop), `item/tool_rules` (relative hardness, `canHarvest`, wear and spend costs), `render/break_overlay` (the destroy stages), generated `data/a1.1.2/harvest.json`; the R branch of `editBlocks` in `src/platform/ctr/main.cpp` | `tests/block_breaking_test.cpp`, `tool_rules_test.cpp`, `break_overlay_test.cpp`, `survival_use_test.cpp`. **Harvest is asked after the wear**, so a tool that breaks on its last block drops nothing. No polygon offset on PICA: the overlay is expanded 0.002 |
| Container screens, crafting, furnace, chest | `item/container_session` (the open screen -- `lo`, `hx`, `id`, `ea` as one slot list), `item/container` (`ee`'s click rules), `item/crafting` (shaped, mirrored first), `tick/furnace` (`ke`), `tick/behaviour` (`blockActivated`, `chestInventoryParts`, `canPlaceAt`'s chest rule), `gui/container_layout` (where the slots are, and the scroll window a chest bigger than a double one gets), generated `data/a1.1.2/recipes.json`; drawing in `src/platform/ctr/hud.cpp`, input in `overlay.cpp`, the sink in `main.cpp` | `tests/container_test.cpp`, `crafting_test.cpp`, `furnace_test.cpp`, `container_session_test.cpp`, `container_layout_test.cpp`; `status.md` 46. **Tile entities are copied, not borrowed** -- the lit/unlit swap reorders the column's vector -- and a screen whose block has gone closes, because a1.1.2 has no `canInteractWith` to copy |
| A block whose texture reads its neighbours (the chest) | `block/world_texture` (the rules), `block/block_def` (the `worldTexture` column), `mesh/mesher` (`emitNonCube`), `tools/configure.py` (which keeps such a block off the fast path), `data/a1.1.2/blocks.json` | `tests/chest_test.cpp`; `status.md` 50. **The one rule in the project that reads a diagonal neighbour**, which is why `WorldStreamer::tickBlockChanged` invalidates the corner section as well |
| Hearts, armour and air on the top screen | `render/hud_mesh` (`lu.a(FZII)`'s layout), `texture/icon_sheet` (the stand-in), `texture/atlas_image` (`iconsRgba`), `Atlas::initIcons`/`bindIcons` and `Renderer::drawHud` | `tests/hud_mesh_test.cpp`, `icon_sheet` cases in `dev_art_test.cpp`. **This is the one deviation from `3ds-performance.md` §11** and owes a hardware fill number; `initIcons` must be called from `Renderer::init` *and* `setAtlas`, which is the item-sheet bug |
| Movement / collision / targeting | `entity/player_body`, `entity/sweep`, `entity/ray_trace`, `block/collision`, `block/fluid_flow` | `tests/player_body_test.cpp`, `ray_trace_test.cpp`, `fluid_push_test.cpp`; `physics-a1.1.2.md` |
| Walking into or standing on an entity, and what a vehicle is stopped by | `entity/entity_boxes` (the world entity list, as boxes), `entity/sweep` (`Mover` and the entity fold), `tick/tick_world` (`forEachSolidBox`); the bind is in `src/platform/ctr/main.cpp` | `tests/entity_boxes_test.cpp`; `physics-a1.1.2.md` *`collidingBoxes` has an entity half*. **Only `dc` (boat) and `oc` (minecart) override `kh.f_()`**, so nothing else is solid, and only those two override `kh.b_(kh)`, so only they collide with everything else. This row is not where a mob's push lives (`entity/mob`, `applyEntityCollision`) |
| What a footstep does to the block underfoot | `tick/behaviour` (`entityWalkedOnBlock`, beside `entityCollidedWithBlocks`), `entity/player_body` (`steppedOn` / `stepBlock{X,Y,Z}`, recorded because `move()` moves against a const world); called from `src/platform/ctr/main.cpp` and `entity/mob` | `tests/trample_test.cpp`, `tests/streamer_revisit_test.cpp` (the render bracket); `status.md` 42. **Two overrides of `ly.a(Lcn;IIILkh;)V` in a1.1.2**: farmland, trampled to dirt on one roll in four, and redstone ore, which lights. One call per footstep, so running and jumping cost what walking does -- the fall-distance version is Beta's. **Anything the frame loop writes outside `stepTicks` needs `WorldStreamer::RenderBracket`** or it is invisible until something else touches the section |
| Ticks / rails / redstone / fluid | `tick/tick_world`, `tick/behaviour`, `tick/rail`, `tick/redstone`, `tick/fluid` | `tests/tick_test.cpp`, `rail_test.cpp`; `tick-a1.1.2.md` |
| Chunk streaming / stale meshes / saving edits | `render/world_streamer`, `world/chunk_cache`, `render/chunk_renderer` | `tests/streamer_*`, `chunk_cache_test.cpp`; run TSan for concurrency changes |
| Folder saves / NBT / player state | `src/impl/storage/alpha_chunkfiles/{chunk_nbt,level_dat,item_nbt}`, `nbt/preserved`, `world/level_data` | `tests/chunk_nbt_test.cpp`, `level_dat_test.cpp`, `storage_test.cpp`; `world-format.md` |
| Packed saves / conversion | `world/any_storage`, `world/format/{packed_storage,manifest,region_file,converter}` | `tests/packed_storage_test.cpp`; `packed-worlds.md`; level data uses the same Alpha codec |
| Block geometry / lighting appearance | `mesh/{mesher,shapes,fluid,torch}`, `block/model`, `world/{lighting,light_update}` | `tests/mesher_test.cpp`, `shapes_test.cpp`; distinguish mesh lighting from world light propagation |
| Greedy meshing / cube atlas / seams | `mesh/{mesher,cube_atlas,vertex}`, `shaders/{world,quad}.v.pica`, `Atlas::initCube`/`bindCube` in `src/platform/ctr/textures.cpp` | `tests/greedy_test.cpp`, `quad_format_test.cpp`; `status.md` §22; a shader change needs the 3DS build, not only the host suite |
| GPU drawing / missing entity models | `src/platform/ctr/renderer.cpp`, `render/*_mesh`, `render/box_model`, `texture/entity_skins` | `tests/box_model_test.cpp`, `entity_skins_test.cpp`; `entity-render-a1.1.2.md`, `3ds-performance.md` |
| Sky, sun, moon, stars, and what colour the fog is | `world/daylight` (`skyColour`, `fogColour`, `starBrightness`, `viewFogColour` -- beside `celestialAngle`), `render/sky` (the geometry, built once), `Renderer::drawSky` / `setWorldTime` / `skyViewProjection` in `src/platform/ctr/renderer.cpp`, `texture/entity_skins` (the sun and moon pages); the per-frame call is beside `setSkyDarken` in `src/platform/ctr/main.cpp` | `tests/sky_test.cpp` against `tests/sky_vectors.hpp` (`genref.java --sky`, off a real World); `status.md` 43. **a1.1.2 has no biome tint in the sky** -- one world field, `0x88BBFF`, and no dome either: the sky is a flat plane sixteen blocks up and the fog fade across it *is* the horizon. The clear colour and the fog colour must stay the same colour or the horizon shows a seam. **Clouds are not ported** |
| Particles (any puff, spark, splash or fleck) | `entity/particle` (the twelve kinds and their ticks), `tick/display` (`cn.m` and the seven blocks that answer it), `render/particle_mesh` (the ramps and the three spans), `texture/particle_sheet`; `Renderer::drawParticles` and `bindParticles` in `src/platform/ctr/main.cpp` | `tests/particle_test.cpp`, `display_tick_test.cpp`, `particle_mesh_test.cpp`; `status.md` 29. Every spawn goes through `TickWorld::spawnParticle`, so **grep that** to find who throws what. Two kinds are spawned by nothing and both say why in the header |
| Texture packs / animated textures | `texture/{atlas_image,dev_art,tiled,texture_fx,fluid_fx,compass_fx}`, `src/platform/ctr/textures.cpp` | `tests/pack_test.cpp`, `tiled_test.cpp`, `texture_fx_test.cpp`, `fluid_fx_test.cpp`, `compass_test.cpp`; `assets.md`. The six `TextureFX` a1.1.2 registers against terrain.png are **generated, never read from the pack**: two flames in `texture_fx`, water/lava still and flowing in `fluid_fx` |
| Audio | `audio/{sound_engine,block_sound,music_ticker,vorbis_stream}`, `src/platform/ctr/audio.hpp` | `tests/step_sound_test.cpp`; `audio-a1.1.2.md`; TSan for decode-thread changes |
| Menu / HUD / input / map | `src/platform/ctr/{main,menu,overlay,hud,map_screen}`, `gui/`, `map/`, `settings/`; World Settings' size row is `world/size_scan` on the I/O thread (`Menu::measureSelectedWorld`/`pollWorldSize`) | Search matching `tests/*`, and `tests/size_scan_test.cpp`; host harness cannot verify actual 3DS controls/screens |
| Main-menu bottom-screen previews (skin row, pack scene, world diorama) | `src/platform/ctr/menu_preview`, `core/preview/{preview_window,preview_worker,pack_scene,diorama}`, `world/world_peek`, `render/player_model`; hooks `Menu::syncPreview`/`updatePreview`/`drawPreviewScreen` | `tests/{preview_window,preview_worker,pack_scene,diorama,world_peek,player_model}_test.cpp`; main menu only -- the pause menu keeps the console; not verified on hardware |
| World generation | `src/impl/worldgen/alpha_nobiome/`, generated version config | Search `tests/*worldgen*`, `*provider*`, `*populate*`; indexed worldgen sections in `status.md` |
| Creating a world | `Menu::openCreateWorld`/`handleCreateWorld`/`drawCreateWorld`/`createWorld` and the `kCreateRows` table, `world::defaultWorldName`/`sanitizeWorldName`, `seedFromText`, `AnyStorage::create` (the Format row) | `tests/seed_text_test.cpp`, `world_list_test.cpp`, `packed_storage_test.cpp`; the screen itself is 3DS-only. **Nothing is written until Create** -- every row edits `Menu::NewWorld` |
| Per-world settings / Extra Settings | `core/settings/world_settings` (the file), `Menu::handleExtraSettings`/`drawExtraSettings`/`saveExtraLevel` and the `WorldPack`/`MovePanorama` screens, `worldgen::GeneratorOptions` (the two fixes), `WorldStreamer::open` (which reads them), `preview::dioramaOrigin` + `MenuPreview::forgetWorld` (the panorama) | `tests/world_settings_test.cpp`, `ore_test.cpp`, `terrain_test.cpp`, `diorama_test.cpp`, `streamer_generate_test.cpp`; `status.md` 39 and `worldgen-a1.1.2.md` *The two generator bugs*. Gamemode/difficulty are the same file; the Extra rows are the deliberate non-vanilla ones |
| Build / version data / reference derivation | `CMakeLists.txt`, `Makefile`, `versions/`, `tools/{configure.py,extract_blocks.py,javap.py,genref.java}` | `build-versions.md`, `working-guide.md`; jar derivation is a maintainer action, never a runtime prerequisite |

## Bounded discovery

```sh
rg -n 'snapshotEntities|saveNow' src/core/render/world_streamer.cpp
rg -n '^TEST.*(save|entity)' tests/entity_persistence_test.cpp
rg -n -i 'minecart|persistence' docs/code-map.md docs/doc-index.md
# Use the returned section range, for example:
sed -n '1,75p' src/core/entity/persistence.hpp
```

`src/platform/ctr/main.cpp` owns game-loop wiring. Core logic may be correct but never called;
trace placement → pool → tick → rendering or save, as appropriate. Test file names and generated
indexes are discovery aids, not proof of coverage. Avoid full-tree `find`, build-directory scans,
generated vector dumps, and whole-file reads of the large status/physics/rendering documents.
